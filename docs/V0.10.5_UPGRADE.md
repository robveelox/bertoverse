# Historical: upgrade Bertoverse to v0.10.5

This release note is retained for history. For a current install or upgrade, use [V0.10.6_UPGRADE.md](V0.10.6_UPGRADE.md).

The original deployment sequence was: back up `/var/www/greenroom` and the `greenroom` database, upload the release ZIP to `/home/administrator/`, extract without replacing `.env`, run `npm ci` and `npm run build` as `greenroom`, restart `greenroom.service`, then verify `/health` through the HTTPS Nginx vhost. Keep this page as historical context; use the current migration list rather than copying commands from this note.
